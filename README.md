# 🧮 Java Calculator App (Dockerized)

A simple command-line calculator built with Java and containerized using Docker.

## 🚀 Build the Docker Image

```bash
docker build -t java-calculator-app .
